@page "/logout"
@attribute [Authorize]
@rendermode InteractiveServer

@code {
    protected override Task OnInitializedAsync()
    {
        NavigationManager.NavigateTo("/logout/ssr", true);
        return base.OnInitializedAsync();
    }
}